<%= render 'spree/admin/shared/accounts_accordion' %>

<div id="acc_content">

    <table class="index">
      <tr>
        <th>Contact type</th>
        <th></th>
        <th></th>
      </tr>

    <% @admin_contact_types.each do |admin_contact_type| %>
      <tr>
        <td><%= admin_contact_type.contact_type %></td>
        <td><%= link_to 'Edit', edit_admin_contact_type_path(admin_contact_type) %></td>
        <td><%= link_to 'Destroy', admin_contact_type, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
    <% end %>
    </table>

    <br />

    <%= link_to 'New Contact type', new_admin_contact_type_path %>

</div>
