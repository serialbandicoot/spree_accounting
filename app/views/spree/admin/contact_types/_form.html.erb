<%= form_for(@admin_contact_type) do |f| %>
  <% if @admin_contact_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_contact_type.errors.count, "error") %> prohibited this admin_contact_type from being saved:</h2>

      <ul>
      <% @admin_contact_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_type %><br />
    <%= f.text_field :contact_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
