<%= render 'spree/admin/shared/accounts_accordion' %>

    <div id="acc_content">

    <table>
      <tr>
        <th>Expense category</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

    <% @admin_expense_categories.each do |admin_expense_category| %>
      <tr>
        <td><%= admin_expense_category.expense_category %></td>
        <td><%= link_to 'Show', admin_expense_category %></td>
        <td><%= link_to 'Edit', edit_admin_expense_category_path(admin_expense_category) %></td>
        <td><%= link_to 'Destroy', admin_expense_category, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
    <% end %>
    </table>

    <br />

    <%= link_to 'New Expense category', new_admin_expense_category_path %>

</div>
