<%= form_for(@admin_banking_institution) do |f| %>
  <% if @admin_banking_institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_banking_institution.errors.count, "error") %> prohibited this admin_banking_institution from being saved:</h2>

      <ul>
      <% @admin_banking_institution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :institution_name %><br />
    <%= f.text_field :institution_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
