<%= form_for(@admin_banking_transaction_type) do |f| %>
  <% if @admin_banking_transaction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_banking_transaction_type.errors.count, "error") %> prohibited this admin_banking_transaction_type from being saved:</h2>

      <ul>
      <% @admin_banking_transaction_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :banking_transaction_type %><br />
    <%= f.text_field :banking_transaction_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
