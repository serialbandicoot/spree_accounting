<%= form_for(@admin_social_media_type) do |f| %>
  <% if @admin_social_media_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_social_media_type.errors.count, "error") %> prohibited this admin_social_media_type from being saved:</h2>

      <ul>
      <% @admin_social_media_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :social_media_type %><br />
    <%= f.text_field :social_media_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
