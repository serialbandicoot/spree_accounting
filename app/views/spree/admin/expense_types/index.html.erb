<%= render 'spree/admin/shared/accounts_accordion' %>

<div id="acc_content">

    <table>
      <tr>
        <th>Expense type</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

    <% @admin_expense_types.each do |admin_expense_type| %>
      <tr>
        <td><%= admin_expense_type.expense_type %></td>
        <td><%= link_to 'Show', admin_expense_type %></td>
        <td><%= link_to 'Edit', edit_admin_expense_type_path(admin_expense_type) %></td>
        <td><%= link_to 'Destroy', admin_expense_type, :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
    </table>

    <br />

    <%= link_to 'New Expense type', new_admin_expense_type_path %>

</div>
