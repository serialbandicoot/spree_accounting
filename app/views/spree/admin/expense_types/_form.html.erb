<%= form_for(@admin_expense_type) do |f| %>
  <% if @admin_expense_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_expense_type.errors.count, "error") %> prohibited this admin_expense_type from being saved:</h2>

      <ul>
      <% @admin_expense_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :expense_type %><br />
    <%= f.text_field :expense_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
