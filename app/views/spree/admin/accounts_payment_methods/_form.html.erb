<%= form_for(@admin_payment_method) do |f| %>
  <% if @admin_payment_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_payment_method.errors.count, "error") %> prohibited this admin_payment_method from being saved:</h2>

      <ul>
      <% @admin_payment_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :payment_type %><br />
    <%= f.text_field :payment_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
